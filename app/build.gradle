plugins { id 'com.android.application' }

android {
    namespace 'com.dim5l.botupdater'
    compileSdk 35

    defaultConfig {
        applicationId 'com.dim5l.botupdater'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '1.1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
